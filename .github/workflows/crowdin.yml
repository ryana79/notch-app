name: Crowdin Sync

on:
  workflow_dispatch:

jobs:
  crowdin:
    runs-on: ubuntu-latest
    steps:
      - name: Crowdin not configured
        run: echo "Crowdin sync is disabled for NotchPro. Configure secrets and update crowdin.yml to enable."
